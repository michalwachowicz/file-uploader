<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | File Uploader</title>
  </head>
  <body>
    <h1>Login</h1>

    <form action="/auth/login" method="post">
      <%- include("partials/field", { name: "username", label: "Username", errors: locals.errors }) %>
      <%- include("partials/field", { name: "password", label: "Password", type: "password", errors: locals.errors }) %>

      <% if (locals.errors && locals.errors._error) { %>
        <div class="error"><%= locals.errors._error %></div>
      <% } %>

      <button type="submit">Login</button>
    </form>

    <div>Don't have an account? <a href="/auth/register">Register</a></div>
  </body>
</html>
